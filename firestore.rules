rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }

    match /emailing/{emailDoc} {
      allow create, list: if true;
      allow read, update, delete: if false;
    }
  }
}